version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - gofmt
    - goimports
    - misspell
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck

formatters:
  enable:
    - gofmt
